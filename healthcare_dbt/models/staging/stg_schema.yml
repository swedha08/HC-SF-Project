version: 2

models:
  - name: stg_patients
    columns:
      - name: patient_id
        tests:
          - unique
          - not_null

  - name: stg_encounters
    columns:
      - name: encounter_id
        tests:
          - unique
          - not_null
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('stg_patients')
              field: patient_id